<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.express_gc">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/fengmian"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity"
            android:theme="@style/AppWelcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:label="MainActivity"></activity>

        <activity
            android:name=".Receive_orderActivity"
            android:label="Receive_orderActivity" />
        <activity
            android:name=".SendActivity"
            android:label="SendActivity" />
        <activity
            android:name=".UsersActivity"
            android:label="@string/title_activity_users" />
        <activity
            android:name=".DengluActivity"
            android:label="@string/title_activity_denglu" />
        <activity
            android:name=".ZhuceActivity"
            android:label="@string/title_activity_zhuce" />
        <activity
            android:name=".ZhmmActivity"
            android:label="@string/title_activity_zhmm" />
        <activity
            android:name=".XmmActivity"
            android:label="@string/title_activity_xmm" />
        <activity
            android:name=".ChaxunActivity"
            android:label="@string/title_activity_chaxun" />
        <activity android:name=".ChakanActivity"/>
        <activity
            android:name=".aboutAuthorActivity"
            android:label="@string/title_activity_about_author"></activity>


    </application>

</manifest>